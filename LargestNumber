package arrays;

public class Numbers {

	public static void main(String[] args) {

    int[] num = { 2,3,99,5,6,79,8,9 };
		int max = num[0];
		
		
		for (int i = 0; i < num.length; i++) {
			if(max<num[i]) {
				max=num[i];
			}
		}
		System.out.println("Largest number "+max);

	//smallest number

//		int min = num[0];
//		for (int i = 0; i < num.length; i++) {
//			if(min>num[i]) {
//				min=num[i];
//			}
//		}
//		System.out.println("Smallest number "+min);
		

	}
}
